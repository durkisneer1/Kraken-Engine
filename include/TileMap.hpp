#pragma once

#include "Math.hpp"
#include <string>
#include <vector>
#include <map>


namespace kn {
    /// @brief A class that represents a tile map.
    /// @details This class reads from TMX files generated by the Tiled map editor.
    class TileMap {
    public:
        TileMap(const std::string &filePath, kn::math::Vector2 tileSize);
        ~TileMap() = default;

    private:
        kn::math::Vector2 tileSize;
    };
}
